<div class="container-fluid form_a">
  <div class="row-fluid">
  <div class="span6">
    <h1><%= @person.name %> <%= @person.surname %> </h1>
    <table class="table">
      <tr>
        <td>First Name: <%= @person.name %><td>Surname: <%= @person.surname%>
      </tr>
      <tr>
        <td>Address: <%= @person.address %><td><td> State: <%= @person.state%></td>
      </tr>
      <tr>
        <td>Email: <%= @person.email%><td><td>Mobile: <%= @person.mobile %><td>
      </tr>
      <tr>
        <td>Type: <%= @person.type %><td><td>Status: <%= @person.status %><td>
      </tr>
      <button><%= link_to 'Edit', edit_person_path(@person) %></button>
      <button><%= link_to 'Delete', @person, :method => :delete %></button>
      </p>
    </table>
  </div>
<div class="container-fluid form_b">
  <div class="row-fluid">
    <div class="span6">
      <h1>CALENDAR</h1>
      <div id="google"><iframe name="google" src="https://www.google.com/calendar/embed?src=qetsbt61e7bjh7iagu50tvia1c%40group.calendar.google.com&ctz=Australia/Sydney"></iframe></div>
    </div>
  <div class="row-fluid">
    <div class="span6">
      <h1>EVENT NOTES</h1>
      <table class="table">
      <tr><td><%=@person.id%><td><td>  <td><td>   <td></tr>
  </div>
</div>

